type T = struct { x : int; y : bool };

type U = struct { x : T };

function g (a : T) returns (y : U);
let
  y = U { x = a };
tel

function f (x : T) returns (y : int)
let
  y = if x.y then x.x else 0;
tel

function main () returns (y : int)
var z : T;
let
  --%MAIN;
  z = T { x = 0; y = true };
  y = f (z);
tel
